version: "3.9"

services:
  web:
    build: .
    expose:
      - "8000"
    volumes:
      - ./app:/code/app
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web
    restart: always
